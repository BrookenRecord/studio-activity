--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require("../../proto")
local typeRegistry = require("../../proto/typeRegistry")

type _Messages = {
	TelemetryRequest: _TelemetryRequestMessage,
	PluginLoaded: _PluginLoadedMessage,
	UiOpened: _UiOpenedMessage,
	OnboardingCompleted: _OnboardingCompletedMessage,
	AccountLinkStarted: _AccountLinkStartedMessage,
	AccountLinked: _AccountLinkedMessage,
	DeviceCodeFlowFailed: _DeviceCodeFlowFailedMessage,
	BrowserFlowFailed: _BrowserFlowFailedMessage,
	AccountRemoved: _AccountRemovedMessage,
	PresenceToggled: _PresenceToggledMessage,
	ProfileSelected: _ProfileSelectedMessage,
	SessionError: _SessionErrorMessage,
	PluginUnloaded: _PluginUnloadedMessage,
	BrowserFlowStartRequest: _BrowserFlowStartRequestMessage,
	BrowserFlowStartResponse: _BrowserFlowStartResponseMessage,
	BrowserFlowPollRequest: _BrowserFlowPollRequestMessage,
	BrowserFlowPollResponse: _BrowserFlowPollResponseMessage,
	AccountLinkFlow: _AccountLinkFlowMessage,
	BrowserFlowStatus: _BrowserFlowStatusMessage,
}
local messages: _Messages = {} :: _Messages

type _TelemetryRequestImpl = {
	__index: _TelemetryRequestImpl,
	new: (fields: _TelemetryRequestPartialFields?) -> TelemetryRequest,
	encode: (self: TelemetryRequest) -> buffer,
	decode: (input: buffer) -> TelemetryRequest,
	jsonEncode: (self: TelemetryRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> TelemetryRequest,
	descriptor: proto.Descriptor,
}

type _TelemetryRequestFields = {
	distinctId: string,
	pluginVersion: string,
	pluginChannel: string,
	pluginHash: string,
	sessionId: string,
	event: (
		{ type: "pluginLoaded", value: PluginLoaded }
		| { type: "uiOpened", value: UiOpened }
		| { type: "onboardingCompleted", value: OnboardingCompleted }
		| { type: "accountLinkStarted", value: AccountLinkStarted }
		| { type: "accountLinked", value: AccountLinked }
		| { type: "deviceCodeFlowFailed", value: DeviceCodeFlowFailed }
		| { type: "browserFlowFailed", value: BrowserFlowFailed }
		| { type: "accountRemoved", value: AccountRemoved }
		| { type: "presenceToggled", value: PresenceToggled }
		| { type: "profileSelected", value: ProfileSelected }
		| { type: "sessionError", value: SessionError }
		| { type: "pluginUnloaded", value: PluginUnloaded }
	)?,
}

type _TelemetryRequestPartialFields = {
	distinctId: string?,
	pluginVersion: string?,
	pluginChannel: string?,
	pluginHash: string?,
	sessionId: string?,
	event: (
		{ type: "pluginLoaded", value: PluginLoaded }
		| { type: "uiOpened", value: UiOpened }
		| { type: "onboardingCompleted", value: OnboardingCompleted }
		| { type: "accountLinkStarted", value: AccountLinkStarted }
		| { type: "accountLinked", value: AccountLinked }
		| { type: "deviceCodeFlowFailed", value: DeviceCodeFlowFailed }
		| { type: "browserFlowFailed", value: BrowserFlowFailed }
		| { type: "accountRemoved", value: AccountRemoved }
		| { type: "presenceToggled", value: PresenceToggled }
		| { type: "profileSelected", value: ProfileSelected }
		| { type: "sessionError", value: SessionError }
		| { type: "pluginUnloaded", value: PluginUnloaded }
	)?,
}

export type TelemetryRequest = typeof(setmetatable({} :: _TelemetryRequestFields, {} :: _TelemetryRequestImpl))
type _TelemetryRequestMessage = proto.Message<TelemetryRequest, _TelemetryRequestPartialFields>

type _PluginLoadedImpl = {
	__index: _PluginLoadedImpl,
	new: (fields: _PluginLoadedPartialFields?) -> PluginLoaded,
	encode: (self: PluginLoaded) -> buffer,
	decode: (input: buffer) -> PluginLoaded,
	jsonEncode: (self: PluginLoaded) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> PluginLoaded,
	descriptor: proto.Descriptor,
}

type _PluginLoadedFields = {
	accountCount: number,
	isPresenceActive: boolean,
	activeProfile: string,
}

type _PluginLoadedPartialFields = {
	accountCount: number?,
	isPresenceActive: boolean?,
	activeProfile: string?,
}

export type PluginLoaded = typeof(setmetatable({} :: _PluginLoadedFields, {} :: _PluginLoadedImpl))
type _PluginLoadedMessage = proto.Message<PluginLoaded, _PluginLoadedPartialFields>

type _UiOpenedImpl = {
	__index: _UiOpenedImpl,
	new: (fields: _UiOpenedPartialFields?) -> UiOpened,
	encode: (self: UiOpened) -> buffer,
	decode: (input: buffer) -> UiOpened,
	jsonEncode: (self: UiOpened) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> UiOpened,
	descriptor: proto.Descriptor,
}

type _UiOpenedFields = {}

type _UiOpenedPartialFields = {}

export type UiOpened = typeof(setmetatable({} :: _UiOpenedFields, {} :: _UiOpenedImpl))
type _UiOpenedMessage = proto.Message<UiOpened, _UiOpenedPartialFields>

type _OnboardingCompletedImpl = {
	__index: _OnboardingCompletedImpl,
	new: (fields: _OnboardingCompletedPartialFields?) -> OnboardingCompleted,
	encode: (self: OnboardingCompleted) -> buffer,
	decode: (input: buffer) -> OnboardingCompleted,
	jsonEncode: (self: OnboardingCompleted) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> OnboardingCompleted,
	descriptor: proto.Descriptor,
}

type _OnboardingCompletedFields = {
	optedIntoTelemetry: boolean,
}

type _OnboardingCompletedPartialFields = {
	optedIntoTelemetry: boolean?,
}

export type OnboardingCompleted = typeof(setmetatable({} :: _OnboardingCompletedFields, {} :: _OnboardingCompletedImpl))
type _OnboardingCompletedMessage = proto.Message<OnboardingCompleted, _OnboardingCompletedPartialFields>

type _AccountLinkStartedImpl = {
	__index: _AccountLinkStartedImpl,
	new: (fields: _AccountLinkStartedPartialFields?) -> AccountLinkStarted,
	encode: (self: AccountLinkStarted) -> buffer,
	decode: (input: buffer) -> AccountLinkStarted,
	jsonEncode: (self: AccountLinkStarted) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> AccountLinkStarted,
	descriptor: proto.Descriptor,
}

type _AccountLinkStartedFields = {}

type _AccountLinkStartedPartialFields = {}

export type AccountLinkStarted = typeof(setmetatable({} :: _AccountLinkStartedFields, {} :: _AccountLinkStartedImpl))
type _AccountLinkStartedMessage = proto.Message<AccountLinkStarted, _AccountLinkStartedPartialFields>

type _AccountLinkedImpl = {
	__index: _AccountLinkedImpl,
	new: (fields: _AccountLinkedPartialFields?) -> AccountLinked,
	encode: (self: AccountLinked) -> buffer,
	decode: (input: buffer) -> AccountLinked,
	jsonEncode: (self: AccountLinked) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> AccountLinked,
	descriptor: proto.Descriptor,
}

type _AccountLinkedFields = {
	accountCount: number,
	isFirstAccount: boolean,
	linkFlow: AccountLinkFlow,
}

type _AccountLinkedPartialFields = {
	accountCount: number?,
	isFirstAccount: boolean?,
	linkFlow: AccountLinkFlow?,
}

export type AccountLinked = typeof(setmetatable({} :: _AccountLinkedFields, {} :: _AccountLinkedImpl))
type _AccountLinkedMessage = proto.Message<AccountLinked, _AccountLinkedPartialFields>

type _DeviceCodeFlowFailedImpl = {
	__index: _DeviceCodeFlowFailedImpl,
	new: (fields: _DeviceCodeFlowFailedPartialFields?) -> DeviceCodeFlowFailed,
	encode: (self: DeviceCodeFlowFailed) -> buffer,
	decode: (input: buffer) -> DeviceCodeFlowFailed,
	jsonEncode: (self: DeviceCodeFlowFailed) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> DeviceCodeFlowFailed,
	descriptor: proto.Descriptor,
}

type _DeviceCodeFlowFailedFields = {
	error: string,
}

type _DeviceCodeFlowFailedPartialFields = {
	error: string?,
}

export type DeviceCodeFlowFailed = typeof(setmetatable(
	{} :: _DeviceCodeFlowFailedFields,
	{} :: _DeviceCodeFlowFailedImpl
))
type _DeviceCodeFlowFailedMessage = proto.Message<DeviceCodeFlowFailed, _DeviceCodeFlowFailedPartialFields>

type _BrowserFlowFailedImpl = {
	__index: _BrowserFlowFailedImpl,
	new: (fields: _BrowserFlowFailedPartialFields?) -> BrowserFlowFailed,
	encode: (self: BrowserFlowFailed) -> buffer,
	decode: (input: buffer) -> BrowserFlowFailed,
	jsonEncode: (self: BrowserFlowFailed) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> BrowserFlowFailed,
	descriptor: proto.Descriptor,
}

type _BrowserFlowFailedFields = {
	error: string,
}

type _BrowserFlowFailedPartialFields = {
	error: string?,
}

export type BrowserFlowFailed = typeof(setmetatable({} :: _BrowserFlowFailedFields, {} :: _BrowserFlowFailedImpl))
type _BrowserFlowFailedMessage = proto.Message<BrowserFlowFailed, _BrowserFlowFailedPartialFields>

type _AccountRemovedImpl = {
	__index: _AccountRemovedImpl,
	new: (fields: _AccountRemovedPartialFields?) -> AccountRemoved,
	encode: (self: AccountRemoved) -> buffer,
	decode: (input: buffer) -> AccountRemoved,
	jsonEncode: (self: AccountRemoved) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> AccountRemoved,
	descriptor: proto.Descriptor,
}

type _AccountRemovedFields = {
	accountCount: number,
}

type _AccountRemovedPartialFields = {
	accountCount: number?,
}

export type AccountRemoved = typeof(setmetatable({} :: _AccountRemovedFields, {} :: _AccountRemovedImpl))
type _AccountRemovedMessage = proto.Message<AccountRemoved, _AccountRemovedPartialFields>

type _PresenceToggledImpl = {
	__index: _PresenceToggledImpl,
	new: (fields: _PresenceToggledPartialFields?) -> PresenceToggled,
	encode: (self: PresenceToggled) -> buffer,
	decode: (input: buffer) -> PresenceToggled,
	jsonEncode: (self: PresenceToggled) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> PresenceToggled,
	descriptor: proto.Descriptor,
}

type _PresenceToggledFields = {
	isActive: boolean,
}

type _PresenceToggledPartialFields = {
	isActive: boolean?,
}

export type PresenceToggled = typeof(setmetatable({} :: _PresenceToggledFields, {} :: _PresenceToggledImpl))
type _PresenceToggledMessage = proto.Message<PresenceToggled, _PresenceToggledPartialFields>

type _ProfileSelectedImpl = {
	__index: _ProfileSelectedImpl,
	new: (fields: _ProfileSelectedPartialFields?) -> ProfileSelected,
	encode: (self: ProfileSelected) -> buffer,
	decode: (input: buffer) -> ProfileSelected,
	jsonEncode: (self: ProfileSelected) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ProfileSelected,
	descriptor: proto.Descriptor,
}

type _ProfileSelectedFields = {
	profile: string,
}

type _ProfileSelectedPartialFields = {
	profile: string?,
}

export type ProfileSelected = typeof(setmetatable({} :: _ProfileSelectedFields, {} :: _ProfileSelectedImpl))
type _ProfileSelectedMessage = proto.Message<ProfileSelected, _ProfileSelectedPartialFields>

type _SessionErrorImpl = {
	__index: _SessionErrorImpl,
	new: (fields: _SessionErrorPartialFields?) -> SessionError,
	encode: (self: SessionError) -> buffer,
	decode: (input: buffer) -> SessionError,
	jsonEncode: (self: SessionError) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SessionError,
	descriptor: proto.Descriptor,
}

type _SessionErrorFields = {
	error: string,
}

type _SessionErrorPartialFields = {
	error: string?,
}

export type SessionError = typeof(setmetatable({} :: _SessionErrorFields, {} :: _SessionErrorImpl))
type _SessionErrorMessage = proto.Message<SessionError, _SessionErrorPartialFields>

type _PluginUnloadedImpl = {
	__index: _PluginUnloadedImpl,
	new: (fields: _PluginUnloadedPartialFields?) -> PluginUnloaded,
	encode: (self: PluginUnloaded) -> buffer,
	decode: (input: buffer) -> PluginUnloaded,
	jsonEncode: (self: PluginUnloaded) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> PluginUnloaded,
	descriptor: proto.Descriptor,
}

type _PluginUnloadedFields = {}

type _PluginUnloadedPartialFields = {}

export type PluginUnloaded = typeof(setmetatable({} :: _PluginUnloadedFields, {} :: _PluginUnloadedImpl))
type _PluginUnloadedMessage = proto.Message<PluginUnloaded, _PluginUnloadedPartialFields>

type _BrowserFlowStartRequestImpl = {
	__index: _BrowserFlowStartRequestImpl,
	new: (fields: _BrowserFlowStartRequestPartialFields?) -> BrowserFlowStartRequest,
	encode: (self: BrowserFlowStartRequest) -> buffer,
	decode: (input: buffer) -> BrowserFlowStartRequest,
	jsonEncode: (self: BrowserFlowStartRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> BrowserFlowStartRequest,
	descriptor: proto.Descriptor,
}

type _BrowserFlowStartRequestFields = {
	codeChallenge: string,
}

type _BrowserFlowStartRequestPartialFields = {
	codeChallenge: string?,
}

export type BrowserFlowStartRequest = typeof(setmetatable(
	{} :: _BrowserFlowStartRequestFields,
	{} :: _BrowserFlowStartRequestImpl
))
type _BrowserFlowStartRequestMessage = proto.Message<BrowserFlowStartRequest, _BrowserFlowStartRequestPartialFields>

type _BrowserFlowStartResponseImpl = {
	__index: _BrowserFlowStartResponseImpl,
	new: (fields: _BrowserFlowStartResponsePartialFields?) -> BrowserFlowStartResponse,
	encode: (self: BrowserFlowStartResponse) -> buffer,
	decode: (input: buffer) -> BrowserFlowStartResponse,
	jsonEncode: (self: BrowserFlowStartResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> BrowserFlowStartResponse,
	descriptor: proto.Descriptor,
}

type _BrowserFlowStartResponseFields = {
	sessionToken: string,
	startUrl: string,
	redirectUri: string,
	expiresIn: number,
	pollInterval: number,
}

type _BrowserFlowStartResponsePartialFields = {
	sessionToken: string?,
	startUrl: string?,
	redirectUri: string?,
	expiresIn: number?,
	pollInterval: number?,
}

export type BrowserFlowStartResponse = typeof(setmetatable(
	{} :: _BrowserFlowStartResponseFields,
	{} :: _BrowserFlowStartResponseImpl
))
type _BrowserFlowStartResponseMessage = proto.Message<BrowserFlowStartResponse, _BrowserFlowStartResponsePartialFields>

type _BrowserFlowPollRequestImpl = {
	__index: _BrowserFlowPollRequestImpl,
	new: (fields: _BrowserFlowPollRequestPartialFields?) -> BrowserFlowPollRequest,
	encode: (self: BrowserFlowPollRequest) -> buffer,
	decode: (input: buffer) -> BrowserFlowPollRequest,
	jsonEncode: (self: BrowserFlowPollRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> BrowserFlowPollRequest,
	descriptor: proto.Descriptor,
}

type _BrowserFlowPollRequestFields = {
	sessionToken: string,
}

type _BrowserFlowPollRequestPartialFields = {
	sessionToken: string?,
}

export type BrowserFlowPollRequest = typeof(setmetatable(
	{} :: _BrowserFlowPollRequestFields,
	{} :: _BrowserFlowPollRequestImpl
))
type _BrowserFlowPollRequestMessage = proto.Message<BrowserFlowPollRequest, _BrowserFlowPollRequestPartialFields>

type _BrowserFlowPollResponseImpl = {
	__index: _BrowserFlowPollResponseImpl,
	new: (fields: _BrowserFlowPollResponsePartialFields?) -> BrowserFlowPollResponse,
	encode: (self: BrowserFlowPollResponse) -> buffer,
	decode: (input: buffer) -> BrowserFlowPollResponse,
	jsonEncode: (self: BrowserFlowPollResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> BrowserFlowPollResponse,
	descriptor: proto.Descriptor,
}

type _BrowserFlowPollResponseFields = {
	status: BrowserFlowStatus,
	code: string,
}

type _BrowserFlowPollResponsePartialFields = {
	status: BrowserFlowStatus?,
	code: string?,
}

export type BrowserFlowPollResponse = typeof(setmetatable(
	{} :: _BrowserFlowPollResponseFields,
	{} :: _BrowserFlowPollResponseImpl
))
type _BrowserFlowPollResponseMessage = proto.Message<BrowserFlowPollResponse, _BrowserFlowPollResponsePartialFields>

type _AccountLinkFlowMessage = proto.Enum<AccountLinkFlow>
export type AccountLinkFlow =
	"ACCOUNT_LINK_FLOW_UNSPECIFIED"
	| "ACCOUNT_LINK_FLOW_DEVICE_CODE"
	| "ACCOUNT_LINK_FLOW_BROWSER"
	| number -- Unknown

type _BrowserFlowStatusMessage = proto.Enum<BrowserFlowStatus>
export type BrowserFlowStatus =
	"BROWSER_FLOW_STATUS_UNSPECIFIED"
	| "BROWSER_FLOW_STATUS_PENDING"
	| "BROWSER_FLOW_STATUS_COMPLETE"
	| "BROWSER_FLOW_STATUS_DENIED"
	| "BROWSER_FLOW_STATUS_EXPIRED"
	| number -- Unknown

do
	local _TelemetryRequestImpl = {}
	_TelemetryRequestImpl.__index = _TelemetryRequestImpl

	function _TelemetryRequestImpl.new(data: _TelemetryRequestPartialFields?): TelemetryRequest
		return setmetatable({
			distinctId = if data == nil or data.distinctId == nil then "" else data.distinctId,
			pluginVersion = if data == nil or data.pluginVersion == nil then "" else data.pluginVersion,
			pluginChannel = if data == nil or data.pluginChannel == nil then "" else data.pluginChannel,
			pluginHash = if data == nil or data.pluginHash == nil then "" else data.pluginHash,
			sessionId = if data == nil or data.sessionId == nil then "" else data.sessionId,
			event = if data == nil or data.event == nil then nil else data.event,
		}, _TelemetryRequestImpl :: _TelemetryRequestImpl)
	end

	function _TelemetryRequestImpl.encode(self: TelemetryRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.distinctId ~= nil and self.distinctId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.distinctId)
		end

		if self.pluginVersion ~= nil and self.pluginVersion ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.pluginVersion)
		end

		if self.pluginChannel ~= nil and self.pluginChannel ~= "" then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.pluginChannel)
		end

		if self.pluginHash ~= nil and self.pluginHash ~= "" then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.pluginHash)
		end

		if self.sessionId ~= nil and self.sessionId ~= "" then
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.sessionId)
		end

		if self.event ~= nil then
			if self.event.type == "pluginLoaded" then
				local encoded = self.event.value:encode()
				output, cursor = proto.writeTag(output, cursor, 10, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.event.type == "uiOpened" then
				local encoded = self.event.value:encode()
				output, cursor = proto.writeTag(output, cursor, 11, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.event.type == "onboardingCompleted" then
				local encoded = self.event.value:encode()
				output, cursor = proto.writeTag(output, cursor, 12, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.event.type == "accountLinkStarted" then
				local encoded = self.event.value:encode()
				output, cursor = proto.writeTag(output, cursor, 13, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.event.type == "accountLinked" then
				local encoded = self.event.value:encode()
				output, cursor = proto.writeTag(output, cursor, 14, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.event.type == "deviceCodeFlowFailed" then
				local encoded = self.event.value:encode()
				output, cursor = proto.writeTag(output, cursor, 15, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.event.type == "browserFlowFailed" then
				local encoded = self.event.value:encode()
				output, cursor = proto.writeTag(output, cursor, 21, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.event.type == "accountRemoved" then
				local encoded = self.event.value:encode()
				output, cursor = proto.writeTag(output, cursor, 16, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.event.type == "presenceToggled" then
				local encoded = self.event.value:encode()
				output, cursor = proto.writeTag(output, cursor, 17, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.event.type == "profileSelected" then
				local encoded = self.event.value:encode()
				output, cursor = proto.writeTag(output, cursor, 18, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.event.type == "sessionError" then
				local encoded = self.event.value:encode()
				output, cursor = proto.writeTag(output, cursor, 19, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.event.type == "pluginUnloaded" then
				local encoded = self.event.value:encode()
				output, cursor = proto.writeTag(output, cursor, 20, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _TelemetryRequestImpl.decode(input: buffer): TelemetryRequest
		local self = _TelemetryRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.distinctId = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.pluginVersion = proto.decodeString(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.pluginChannel = proto.decodeString(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.pluginHash = proto.decodeString(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.sessionId = proto.decodeString(value)
					continue
				elseif field == 10 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.event = { type = "pluginLoaded", value = messages.PluginLoaded.decode(value) }
					continue
				elseif field == 11 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.event = { type = "uiOpened", value = messages.UiOpened.decode(value) }
					continue
				elseif field == 12 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.event = { type = "onboardingCompleted", value = messages.OnboardingCompleted.decode(value) }
					continue
				elseif field == 13 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.event = { type = "accountLinkStarted", value = messages.AccountLinkStarted.decode(value) }
					continue
				elseif field == 14 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.event = { type = "accountLinked", value = messages.AccountLinked.decode(value) }
					continue
				elseif field == 15 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.event = { type = "deviceCodeFlowFailed", value = messages.DeviceCodeFlowFailed.decode(value) }
					continue
				elseif field == 16 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.event = { type = "accountRemoved", value = messages.AccountRemoved.decode(value) }
					continue
				elseif field == 17 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.event = { type = "presenceToggled", value = messages.PresenceToggled.decode(value) }
					continue
				elseif field == 18 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.event = { type = "profileSelected", value = messages.ProfileSelected.decode(value) }
					continue
				elseif field == 19 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.event = { type = "sessionError", value = messages.SessionError.decode(value) }
					continue
				elseif field == 20 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.event = { type = "pluginUnloaded", value = messages.PluginUnloaded.decode(value) }
					continue
				elseif field == 21 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.event = { type = "browserFlowFailed", value = messages.BrowserFlowFailed.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _TelemetryRequestImpl.jsonEncode(self: TelemetryRequest): any
		local output = {}

		if self.distinctId ~= nil and self.distinctId ~= "" then
			output.distinctId = self.distinctId
		end

		if self.pluginVersion ~= nil and self.pluginVersion ~= "" then
			output.pluginVersion = self.pluginVersion
		end

		if self.pluginChannel ~= nil and self.pluginChannel ~= "" then
			output.pluginChannel = self.pluginChannel
		end

		if self.pluginHash ~= nil and self.pluginHash ~= "" then
			output.pluginHash = self.pluginHash
		end

		if self.sessionId ~= nil and self.sessionId ~= "" then
			output.sessionId = self.sessionId
		end

		if self.event ~= nil then
			if self.event.type == "pluginLoaded" then
				output.pluginLoaded = self.event.value:jsonEncode()
			elseif self.event.type == "uiOpened" then
				output.uiOpened = self.event.value:jsonEncode()
			elseif self.event.type == "onboardingCompleted" then
				output.onboardingCompleted = self.event.value:jsonEncode()
			elseif self.event.type == "accountLinkStarted" then
				output.accountLinkStarted = self.event.value:jsonEncode()
			elseif self.event.type == "accountLinked" then
				output.accountLinked = self.event.value:jsonEncode()
			elseif self.event.type == "deviceCodeFlowFailed" then
				output.deviceCodeFlowFailed = self.event.value:jsonEncode()
			elseif self.event.type == "browserFlowFailed" then
				output.browserFlowFailed = self.event.value:jsonEncode()
			elseif self.event.type == "accountRemoved" then
				output.accountRemoved = self.event.value:jsonEncode()
			elseif self.event.type == "presenceToggled" then
				output.presenceToggled = self.event.value:jsonEncode()
			elseif self.event.type == "profileSelected" then
				output.profileSelected = self.event.value:jsonEncode()
			elseif self.event.type == "sessionError" then
				output.sessionError = self.event.value:jsonEncode()
			elseif self.event.type == "pluginUnloaded" then
				output.pluginUnloaded = self.event.value:jsonEncode()
			end
		end

		return output
	end

	function _TelemetryRequestImpl.jsonDecode(input: { [string]: any }): TelemetryRequest
		local self = _TelemetryRequestImpl.new()

		if input.distinctId ~= nil then
			self.distinctId = input.distinctId
		end

		if input.pluginVersion ~= nil then
			self.pluginVersion = input.pluginVersion
		end

		if input.pluginChannel ~= nil then
			self.pluginChannel = input.pluginChannel
		end

		if input.pluginHash ~= nil then
			self.pluginHash = input.pluginHash
		end

		if input.sessionId ~= nil then
			self.sessionId = input.sessionId
		end

		if input.pluginLoaded ~= nil then
			self.event = { type = "pluginLoaded", value = messages.PluginLoaded.jsonDecode(input.pluginLoaded) }
		end

		if input.uiOpened ~= nil then
			self.event = { type = "uiOpened", value = messages.UiOpened.jsonDecode(input.uiOpened) }
		end

		if input.onboardingCompleted ~= nil then
			self.event = {
				type = "onboardingCompleted",
				value = messages.OnboardingCompleted.jsonDecode(input.onboardingCompleted),
			}
		end

		if input.accountLinkStarted ~= nil then
			self.event = {
				type = "accountLinkStarted",
				value = messages.AccountLinkStarted.jsonDecode(input.accountLinkStarted),
			}
		end

		if input.accountLinked ~= nil then
			self.event = { type = "accountLinked", value = messages.AccountLinked.jsonDecode(input.accountLinked) }
		end

		if input.deviceCodeFlowFailed ~= nil then
			self.event = {
				type = "deviceCodeFlowFailed",
				value = messages.DeviceCodeFlowFailed.jsonDecode(input.deviceCodeFlowFailed),
			}
		end

		if input.browserFlowFailed ~= nil then
			self.event =
				{ type = "browserFlowFailed", value = messages.BrowserFlowFailed.jsonDecode(input.browserFlowFailed) }
		end

		if input.accountRemoved ~= nil then
			self.event = { type = "accountRemoved", value = messages.AccountRemoved.jsonDecode(input.accountRemoved) }
		end

		if input.presenceToggled ~= nil then
			self.event =
				{ type = "presenceToggled", value = messages.PresenceToggled.jsonDecode(input.presenceToggled) }
		end

		if input.profileSelected ~= nil then
			self.event =
				{ type = "profileSelected", value = messages.ProfileSelected.jsonDecode(input.profileSelected) }
		end

		if input.sessionError ~= nil then
			self.event = { type = "sessionError", value = messages.SessionError.jsonDecode(input.sessionError) }
		end

		if input.pluginUnloaded ~= nil then
			self.event = { type = "pluginUnloaded", value = messages.PluginUnloaded.jsonDecode(input.pluginUnloaded) }
		end

		return self
	end

	_TelemetryRequestImpl.descriptor = {
		name = "TelemetryRequest",
		fullName = "api.v1.TelemetryRequest",
	}

	messages.TelemetryRequest = _TelemetryRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.TelemetryRequest)
end

do
	local _PluginLoadedImpl = {}
	_PluginLoadedImpl.__index = _PluginLoadedImpl

	function _PluginLoadedImpl.new(data: _PluginLoadedPartialFields?): PluginLoaded
		return setmetatable({
			accountCount = if data == nil or data.accountCount == nil then 0 else data.accountCount,
			isPresenceActive = if data == nil or data.isPresenceActive == nil then false else data.isPresenceActive,
			activeProfile = if data == nil or data.activeProfile == nil then "" else data.activeProfile,
		}, _PluginLoadedImpl :: _PluginLoadedImpl)
	end

	function _PluginLoadedImpl.encode(self: PluginLoaded): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.accountCount ~= nil and self.accountCount ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.accountCount)
		end

		if self.isPresenceActive then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.isPresenceActive then 1 else 0)
		end

		if self.activeProfile ~= nil and self.activeProfile ~= "" then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.activeProfile)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _PluginLoadedImpl.decode(input: buffer): PluginLoaded
		local self = _PluginLoadedImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.accountCount = value
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.isPresenceActive = value ~= 0
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.activeProfile = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _PluginLoadedImpl.jsonEncode(self: PluginLoaded): any
		local output = {}

		if self.accountCount ~= nil and self.accountCount ~= 0 then
			output.accountCount = self.accountCount
		end

		if self.isPresenceActive then
			output.isPresenceActive = self.isPresenceActive
		end

		if self.activeProfile ~= nil and self.activeProfile ~= "" then
			output.activeProfile = self.activeProfile
		end

		return output
	end

	function _PluginLoadedImpl.jsonDecode(input: { [string]: any }): PluginLoaded
		local self = _PluginLoadedImpl.new()

		if input.accountCount ~= nil then
			self.accountCount = input.accountCount
		end

		if input.isPresenceActive ~= nil then
			self.isPresenceActive = input.isPresenceActive
		end

		if input.activeProfile ~= nil then
			self.activeProfile = input.activeProfile
		end

		return self
	end

	_PluginLoadedImpl.descriptor = {
		name = "PluginLoaded",
		fullName = "api.v1.PluginLoaded",
	}

	messages.PluginLoaded = _PluginLoadedImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.PluginLoaded)
end

do
	local _UiOpenedImpl = {}
	_UiOpenedImpl.__index = _UiOpenedImpl

	function _UiOpenedImpl.new(data: _UiOpenedPartialFields?): UiOpened
		return setmetatable({}, _UiOpenedImpl :: _UiOpenedImpl)
	end

	function _UiOpenedImpl.encode(self: UiOpened): buffer
		local output = buffer.create(0)
		local cursor = 0

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _UiOpenedImpl.decode(input: buffer): UiOpened
		local self = _UiOpenedImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _UiOpenedImpl.jsonEncode(self: UiOpened): any
		local output = {}

		return output
	end

	function _UiOpenedImpl.jsonDecode(input: { [string]: any }): UiOpened
		local self = _UiOpenedImpl.new()

		return self
	end

	_UiOpenedImpl.descriptor = {
		name = "UiOpened",
		fullName = "api.v1.UiOpened",
	}

	messages.UiOpened = _UiOpenedImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.UiOpened)
end

do
	local _OnboardingCompletedImpl = {}
	_OnboardingCompletedImpl.__index = _OnboardingCompletedImpl

	function _OnboardingCompletedImpl.new(data: _OnboardingCompletedPartialFields?): OnboardingCompleted
		return setmetatable({
			optedIntoTelemetry = if data == nil or data.optedIntoTelemetry == nil
				then false
				else data.optedIntoTelemetry,
		}, _OnboardingCompletedImpl :: _OnboardingCompletedImpl)
	end

	function _OnboardingCompletedImpl.encode(self: OnboardingCompleted): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.optedIntoTelemetry then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.optedIntoTelemetry then 1 else 0)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _OnboardingCompletedImpl.decode(input: buffer): OnboardingCompleted
		local self = _OnboardingCompletedImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.optedIntoTelemetry = value ~= 0
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _OnboardingCompletedImpl.jsonEncode(self: OnboardingCompleted): any
		local output = {}

		if self.optedIntoTelemetry then
			output.optedIntoTelemetry = self.optedIntoTelemetry
		end

		return output
	end

	function _OnboardingCompletedImpl.jsonDecode(input: { [string]: any }): OnboardingCompleted
		local self = _OnboardingCompletedImpl.new()

		if input.optedIntoTelemetry ~= nil then
			self.optedIntoTelemetry = input.optedIntoTelemetry
		end

		return self
	end

	_OnboardingCompletedImpl.descriptor = {
		name = "OnboardingCompleted",
		fullName = "api.v1.OnboardingCompleted",
	}

	messages.OnboardingCompleted = _OnboardingCompletedImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.OnboardingCompleted)
end

do
	local _AccountLinkStartedImpl = {}
	_AccountLinkStartedImpl.__index = _AccountLinkStartedImpl

	function _AccountLinkStartedImpl.new(data: _AccountLinkStartedPartialFields?): AccountLinkStarted
		return setmetatable({}, _AccountLinkStartedImpl :: _AccountLinkStartedImpl)
	end

	function _AccountLinkStartedImpl.encode(self: AccountLinkStarted): buffer
		local output = buffer.create(0)
		local cursor = 0

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _AccountLinkStartedImpl.decode(input: buffer): AccountLinkStarted
		local self = _AccountLinkStartedImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _AccountLinkStartedImpl.jsonEncode(self: AccountLinkStarted): any
		local output = {}

		return output
	end

	function _AccountLinkStartedImpl.jsonDecode(input: { [string]: any }): AccountLinkStarted
		local self = _AccountLinkStartedImpl.new()

		return self
	end

	_AccountLinkStartedImpl.descriptor = {
		name = "AccountLinkStarted",
		fullName = "api.v1.AccountLinkStarted",
	}

	messages.AccountLinkStarted = _AccountLinkStartedImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.AccountLinkStarted)
end

do
	local _AccountLinkedImpl = {}
	_AccountLinkedImpl.__index = _AccountLinkedImpl

	function _AccountLinkedImpl.new(data: _AccountLinkedPartialFields?): AccountLinked
		return setmetatable({
			accountCount = if data == nil or data.accountCount == nil then 0 else data.accountCount,
			isFirstAccount = if data == nil or data.isFirstAccount == nil then false else data.isFirstAccount,
			linkFlow = if data == nil or data.linkFlow == nil
				then assert(messages.AccountLinkFlow.fromNumber(0), "Enum has no 0 default")
				else data.linkFlow,
		}, _AccountLinkedImpl :: _AccountLinkedImpl)
	end

	function _AccountLinkedImpl.encode(self: AccountLinked): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.accountCount ~= nil and self.accountCount ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.accountCount)
		end

		if self.isFirstAccount then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.isFirstAccount then 1 else 0)
		end

		if
			self.linkFlow ~= nil
			and (self.linkFlow ~= nil and self.linkFlow ~= 0 or self.linkFlow ~= messages.AccountLinkFlow.fromNumber(0))
		then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, messages.AccountLinkFlow.toNumber(self.linkFlow :: any))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _AccountLinkedImpl.decode(input: buffer): AccountLinked
		local self = _AccountLinkedImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.accountCount = value
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.isFirstAccount = value ~= 0
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.linkFlow = (messages.AccountLinkFlow.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]]
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _AccountLinkedImpl.jsonEncode(self: AccountLinked): any
		local output = {}

		if self.accountCount ~= nil and self.accountCount ~= 0 then
			output.accountCount = self.accountCount
		end

		if self.isFirstAccount then
			output.isFirstAccount = self.isFirstAccount
		end

		if
			self.linkFlow ~= nil
			and (self.linkFlow ~= nil and self.linkFlow ~= 0 or self.linkFlow ~= messages.AccountLinkFlow.fromNumber(0))
		then
			output.linkFlow = if typeof(self.linkFlow) == "number"
				then self.linkFlow
				else messages.AccountLinkFlow.toNumber(self.linkFlow :: any)
		end

		return output
	end

	function _AccountLinkedImpl.jsonDecode(input: { [string]: any }): AccountLinked
		local self = _AccountLinkedImpl.new()

		if input.accountCount ~= nil then
			self.accountCount = input.accountCount
		end

		if input.isFirstAccount ~= nil then
			self.isFirstAccount = input.isFirstAccount
		end

		if input.linkFlow ~= nil then
			self.linkFlow = if typeof(input.linkFlow) == "number"
				then (messages.AccountLinkFlow.fromNumber(input.linkFlow) or input.linkFlow)
				else (messages.AccountLinkFlow.fromName(input.linkFlow) or input.linkFlow)
		end

		return self
	end

	_AccountLinkedImpl.descriptor = {
		name = "AccountLinked",
		fullName = "api.v1.AccountLinked",
	}

	messages.AccountLinked = _AccountLinkedImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.AccountLinked)
end

do
	local _DeviceCodeFlowFailedImpl = {}
	_DeviceCodeFlowFailedImpl.__index = _DeviceCodeFlowFailedImpl

	function _DeviceCodeFlowFailedImpl.new(data: _DeviceCodeFlowFailedPartialFields?): DeviceCodeFlowFailed
		return setmetatable({
			error = if data == nil or data.error == nil then "" else data.error,
		}, _DeviceCodeFlowFailedImpl :: _DeviceCodeFlowFailedImpl)
	end

	function _DeviceCodeFlowFailedImpl.encode(self: DeviceCodeFlowFailed): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.error ~= nil and self.error ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.error)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _DeviceCodeFlowFailedImpl.decode(input: buffer): DeviceCodeFlowFailed
		local self = _DeviceCodeFlowFailedImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.error = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _DeviceCodeFlowFailedImpl.jsonEncode(self: DeviceCodeFlowFailed): any
		local output = {}

		if self.error ~= nil and self.error ~= "" then
			output.error = self.error
		end

		return output
	end

	function _DeviceCodeFlowFailedImpl.jsonDecode(input: { [string]: any }): DeviceCodeFlowFailed
		local self = _DeviceCodeFlowFailedImpl.new()

		if input.error ~= nil then
			self.error = input.error
		end

		return self
	end

	_DeviceCodeFlowFailedImpl.descriptor = {
		name = "DeviceCodeFlowFailed",
		fullName = "api.v1.DeviceCodeFlowFailed",
	}

	messages.DeviceCodeFlowFailed = _DeviceCodeFlowFailedImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.DeviceCodeFlowFailed)
end

do
	local _BrowserFlowFailedImpl = {}
	_BrowserFlowFailedImpl.__index = _BrowserFlowFailedImpl

	function _BrowserFlowFailedImpl.new(data: _BrowserFlowFailedPartialFields?): BrowserFlowFailed
		return setmetatable({
			error = if data == nil or data.error == nil then "" else data.error,
		}, _BrowserFlowFailedImpl :: _BrowserFlowFailedImpl)
	end

	function _BrowserFlowFailedImpl.encode(self: BrowserFlowFailed): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.error ~= nil and self.error ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.error)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _BrowserFlowFailedImpl.decode(input: buffer): BrowserFlowFailed
		local self = _BrowserFlowFailedImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.error = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _BrowserFlowFailedImpl.jsonEncode(self: BrowserFlowFailed): any
		local output = {}

		if self.error ~= nil and self.error ~= "" then
			output.error = self.error
		end

		return output
	end

	function _BrowserFlowFailedImpl.jsonDecode(input: { [string]: any }): BrowserFlowFailed
		local self = _BrowserFlowFailedImpl.new()

		if input.error ~= nil then
			self.error = input.error
		end

		return self
	end

	_BrowserFlowFailedImpl.descriptor = {
		name = "BrowserFlowFailed",
		fullName = "api.v1.BrowserFlowFailed",
	}

	messages.BrowserFlowFailed = _BrowserFlowFailedImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.BrowserFlowFailed)
end

do
	local _AccountRemovedImpl = {}
	_AccountRemovedImpl.__index = _AccountRemovedImpl

	function _AccountRemovedImpl.new(data: _AccountRemovedPartialFields?): AccountRemoved
		return setmetatable({
			accountCount = if data == nil or data.accountCount == nil then 0 else data.accountCount,
		}, _AccountRemovedImpl :: _AccountRemovedImpl)
	end

	function _AccountRemovedImpl.encode(self: AccountRemoved): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.accountCount ~= nil and self.accountCount ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.accountCount)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _AccountRemovedImpl.decode(input: buffer): AccountRemoved
		local self = _AccountRemovedImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.accountCount = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _AccountRemovedImpl.jsonEncode(self: AccountRemoved): any
		local output = {}

		if self.accountCount ~= nil and self.accountCount ~= 0 then
			output.accountCount = self.accountCount
		end

		return output
	end

	function _AccountRemovedImpl.jsonDecode(input: { [string]: any }): AccountRemoved
		local self = _AccountRemovedImpl.new()

		if input.accountCount ~= nil then
			self.accountCount = input.accountCount
		end

		return self
	end

	_AccountRemovedImpl.descriptor = {
		name = "AccountRemoved",
		fullName = "api.v1.AccountRemoved",
	}

	messages.AccountRemoved = _AccountRemovedImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.AccountRemoved)
end

do
	local _PresenceToggledImpl = {}
	_PresenceToggledImpl.__index = _PresenceToggledImpl

	function _PresenceToggledImpl.new(data: _PresenceToggledPartialFields?): PresenceToggled
		return setmetatable({
			isActive = if data == nil or data.isActive == nil then false else data.isActive,
		}, _PresenceToggledImpl :: _PresenceToggledImpl)
	end

	function _PresenceToggledImpl.encode(self: PresenceToggled): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.isActive then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.isActive then 1 else 0)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _PresenceToggledImpl.decode(input: buffer): PresenceToggled
		local self = _PresenceToggledImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.isActive = value ~= 0
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _PresenceToggledImpl.jsonEncode(self: PresenceToggled): any
		local output = {}

		if self.isActive then
			output.isActive = self.isActive
		end

		return output
	end

	function _PresenceToggledImpl.jsonDecode(input: { [string]: any }): PresenceToggled
		local self = _PresenceToggledImpl.new()

		if input.isActive ~= nil then
			self.isActive = input.isActive
		end

		return self
	end

	_PresenceToggledImpl.descriptor = {
		name = "PresenceToggled",
		fullName = "api.v1.PresenceToggled",
	}

	messages.PresenceToggled = _PresenceToggledImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.PresenceToggled)
end

do
	local _ProfileSelectedImpl = {}
	_ProfileSelectedImpl.__index = _ProfileSelectedImpl

	function _ProfileSelectedImpl.new(data: _ProfileSelectedPartialFields?): ProfileSelected
		return setmetatable({
			profile = if data == nil or data.profile == nil then "" else data.profile,
		}, _ProfileSelectedImpl :: _ProfileSelectedImpl)
	end

	function _ProfileSelectedImpl.encode(self: ProfileSelected): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.profile ~= nil and self.profile ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.profile)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ProfileSelectedImpl.decode(input: buffer): ProfileSelected
		local self = _ProfileSelectedImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.profile = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ProfileSelectedImpl.jsonEncode(self: ProfileSelected): any
		local output = {}

		if self.profile ~= nil and self.profile ~= "" then
			output.profile = self.profile
		end

		return output
	end

	function _ProfileSelectedImpl.jsonDecode(input: { [string]: any }): ProfileSelected
		local self = _ProfileSelectedImpl.new()

		if input.profile ~= nil then
			self.profile = input.profile
		end

		return self
	end

	_ProfileSelectedImpl.descriptor = {
		name = "ProfileSelected",
		fullName = "api.v1.ProfileSelected",
	}

	messages.ProfileSelected = _ProfileSelectedImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ProfileSelected)
end

do
	local _SessionErrorImpl = {}
	_SessionErrorImpl.__index = _SessionErrorImpl

	function _SessionErrorImpl.new(data: _SessionErrorPartialFields?): SessionError
		return setmetatable({
			error = if data == nil or data.error == nil then "" else data.error,
		}, _SessionErrorImpl :: _SessionErrorImpl)
	end

	function _SessionErrorImpl.encode(self: SessionError): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.error ~= nil and self.error ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.error)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SessionErrorImpl.decode(input: buffer): SessionError
		local self = _SessionErrorImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.error = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SessionErrorImpl.jsonEncode(self: SessionError): any
		local output = {}

		if self.error ~= nil and self.error ~= "" then
			output.error = self.error
		end

		return output
	end

	function _SessionErrorImpl.jsonDecode(input: { [string]: any }): SessionError
		local self = _SessionErrorImpl.new()

		if input.error ~= nil then
			self.error = input.error
		end

		return self
	end

	_SessionErrorImpl.descriptor = {
		name = "SessionError",
		fullName = "api.v1.SessionError",
	}

	messages.SessionError = _SessionErrorImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SessionError)
end

do
	local _PluginUnloadedImpl = {}
	_PluginUnloadedImpl.__index = _PluginUnloadedImpl

	function _PluginUnloadedImpl.new(data: _PluginUnloadedPartialFields?): PluginUnloaded
		return setmetatable({}, _PluginUnloadedImpl :: _PluginUnloadedImpl)
	end

	function _PluginUnloadedImpl.encode(self: PluginUnloaded): buffer
		local output = buffer.create(0)
		local cursor = 0

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _PluginUnloadedImpl.decode(input: buffer): PluginUnloaded
		local self = _PluginUnloadedImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _PluginUnloadedImpl.jsonEncode(self: PluginUnloaded): any
		local output = {}

		return output
	end

	function _PluginUnloadedImpl.jsonDecode(input: { [string]: any }): PluginUnloaded
		local self = _PluginUnloadedImpl.new()

		return self
	end

	_PluginUnloadedImpl.descriptor = {
		name = "PluginUnloaded",
		fullName = "api.v1.PluginUnloaded",
	}

	messages.PluginUnloaded = _PluginUnloadedImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.PluginUnloaded)
end

do
	local _BrowserFlowStartRequestImpl = {}
	_BrowserFlowStartRequestImpl.__index = _BrowserFlowStartRequestImpl

	function _BrowserFlowStartRequestImpl.new(data: _BrowserFlowStartRequestPartialFields?): BrowserFlowStartRequest
		return setmetatable({
			codeChallenge = if data == nil or data.codeChallenge == nil then "" else data.codeChallenge,
		}, _BrowserFlowStartRequestImpl :: _BrowserFlowStartRequestImpl)
	end

	function _BrowserFlowStartRequestImpl.encode(self: BrowserFlowStartRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.codeChallenge ~= nil and self.codeChallenge ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.codeChallenge)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _BrowserFlowStartRequestImpl.decode(input: buffer): BrowserFlowStartRequest
		local self = _BrowserFlowStartRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.codeChallenge = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _BrowserFlowStartRequestImpl.jsonEncode(self: BrowserFlowStartRequest): any
		local output = {}

		if self.codeChallenge ~= nil and self.codeChallenge ~= "" then
			output.codeChallenge = self.codeChallenge
		end

		return output
	end

	function _BrowserFlowStartRequestImpl.jsonDecode(input: { [string]: any }): BrowserFlowStartRequest
		local self = _BrowserFlowStartRequestImpl.new()

		if input.codeChallenge ~= nil then
			self.codeChallenge = input.codeChallenge
		end

		return self
	end

	_BrowserFlowStartRequestImpl.descriptor = {
		name = "BrowserFlowStartRequest",
		fullName = "api.v1.BrowserFlowStartRequest",
	}

	messages.BrowserFlowStartRequest = _BrowserFlowStartRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.BrowserFlowStartRequest)
end

do
	local _BrowserFlowStartResponseImpl = {}
	_BrowserFlowStartResponseImpl.__index = _BrowserFlowStartResponseImpl

	function _BrowserFlowStartResponseImpl.new(data: _BrowserFlowStartResponsePartialFields?): BrowserFlowStartResponse
		return setmetatable({
			sessionToken = if data == nil or data.sessionToken == nil then "" else data.sessionToken,
			startUrl = if data == nil or data.startUrl == nil then "" else data.startUrl,
			redirectUri = if data == nil or data.redirectUri == nil then "" else data.redirectUri,
			expiresIn = if data == nil or data.expiresIn == nil then 0 else data.expiresIn,
			pollInterval = if data == nil or data.pollInterval == nil then 0 else data.pollInterval,
		}, _BrowserFlowStartResponseImpl :: _BrowserFlowStartResponseImpl)
	end

	function _BrowserFlowStartResponseImpl.encode(self: BrowserFlowStartResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.sessionToken ~= nil and self.sessionToken ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.sessionToken)
		end

		if self.startUrl ~= nil and self.startUrl ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.startUrl)
		end

		if self.redirectUri ~= nil and self.redirectUri ~= "" then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.redirectUri)
		end

		if self.expiresIn ~= nil and self.expiresIn ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.expiresIn)
		end

		if self.pollInterval ~= nil and self.pollInterval ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.pollInterval)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _BrowserFlowStartResponseImpl.decode(input: buffer): BrowserFlowStartResponse
		local self = _BrowserFlowStartResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 4 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.expiresIn = value
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.pollInterval = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.sessionToken = proto.decodeString(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.startUrl = proto.decodeString(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.redirectUri = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _BrowserFlowStartResponseImpl.jsonEncode(self: BrowserFlowStartResponse): any
		local output = {}

		if self.sessionToken ~= nil and self.sessionToken ~= "" then
			output.sessionToken = self.sessionToken
		end

		if self.startUrl ~= nil and self.startUrl ~= "" then
			output.startUrl = self.startUrl
		end

		if self.redirectUri ~= nil and self.redirectUri ~= "" then
			output.redirectUri = self.redirectUri
		end

		if self.expiresIn ~= nil and self.expiresIn ~= 0 then
			output.expiresIn = self.expiresIn
		end

		if self.pollInterval ~= nil and self.pollInterval ~= 0 then
			output.pollInterval = self.pollInterval
		end

		return output
	end

	function _BrowserFlowStartResponseImpl.jsonDecode(input: { [string]: any }): BrowserFlowStartResponse
		local self = _BrowserFlowStartResponseImpl.new()

		if input.sessionToken ~= nil then
			self.sessionToken = input.sessionToken
		end

		if input.startUrl ~= nil then
			self.startUrl = input.startUrl
		end

		if input.redirectUri ~= nil then
			self.redirectUri = input.redirectUri
		end

		if input.expiresIn ~= nil then
			self.expiresIn = input.expiresIn
		end

		if input.pollInterval ~= nil then
			self.pollInterval = input.pollInterval
		end

		return self
	end

	_BrowserFlowStartResponseImpl.descriptor = {
		name = "BrowserFlowStartResponse",
		fullName = "api.v1.BrowserFlowStartResponse",
	}

	messages.BrowserFlowStartResponse = _BrowserFlowStartResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.BrowserFlowStartResponse)
end

do
	local _BrowserFlowPollRequestImpl = {}
	_BrowserFlowPollRequestImpl.__index = _BrowserFlowPollRequestImpl

	function _BrowserFlowPollRequestImpl.new(data: _BrowserFlowPollRequestPartialFields?): BrowserFlowPollRequest
		return setmetatable({
			sessionToken = if data == nil or data.sessionToken == nil then "" else data.sessionToken,
		}, _BrowserFlowPollRequestImpl :: _BrowserFlowPollRequestImpl)
	end

	function _BrowserFlowPollRequestImpl.encode(self: BrowserFlowPollRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.sessionToken ~= nil and self.sessionToken ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.sessionToken)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _BrowserFlowPollRequestImpl.decode(input: buffer): BrowserFlowPollRequest
		local self = _BrowserFlowPollRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.sessionToken = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _BrowserFlowPollRequestImpl.jsonEncode(self: BrowserFlowPollRequest): any
		local output = {}

		if self.sessionToken ~= nil and self.sessionToken ~= "" then
			output.sessionToken = self.sessionToken
		end

		return output
	end

	function _BrowserFlowPollRequestImpl.jsonDecode(input: { [string]: any }): BrowserFlowPollRequest
		local self = _BrowserFlowPollRequestImpl.new()

		if input.sessionToken ~= nil then
			self.sessionToken = input.sessionToken
		end

		return self
	end

	_BrowserFlowPollRequestImpl.descriptor = {
		name = "BrowserFlowPollRequest",
		fullName = "api.v1.BrowserFlowPollRequest",
	}

	messages.BrowserFlowPollRequest = _BrowserFlowPollRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.BrowserFlowPollRequest)
end

do
	local _BrowserFlowPollResponseImpl = {}
	_BrowserFlowPollResponseImpl.__index = _BrowserFlowPollResponseImpl

	function _BrowserFlowPollResponseImpl.new(data: _BrowserFlowPollResponsePartialFields?): BrowserFlowPollResponse
		return setmetatable({
			status = if data == nil or data.status == nil
				then assert(messages.BrowserFlowStatus.fromNumber(0), "Enum has no 0 default")
				else data.status,
			code = if data == nil or data.code == nil then "" else data.code,
		}, _BrowserFlowPollResponseImpl :: _BrowserFlowPollResponseImpl)
	end

	function _BrowserFlowPollResponseImpl.encode(self: BrowserFlowPollResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if
			self.status ~= nil
			and (self.status ~= nil and self.status ~= 0 or self.status ~= messages.BrowserFlowStatus.fromNumber(0))
		then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, messages.BrowserFlowStatus.toNumber(self.status :: any))
		end

		if self.code ~= nil and self.code ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.code)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _BrowserFlowPollResponseImpl.decode(input: buffer): BrowserFlowPollResponse
		local self = _BrowserFlowPollResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.status = (messages.BrowserFlowStatus.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]]
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.code = proto.decodeString(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _BrowserFlowPollResponseImpl.jsonEncode(self: BrowserFlowPollResponse): any
		local output = {}

		if
			self.status ~= nil
			and (self.status ~= nil and self.status ~= 0 or self.status ~= messages.BrowserFlowStatus.fromNumber(0))
		then
			output.status = if typeof(self.status) == "number"
				then self.status
				else messages.BrowserFlowStatus.toNumber(self.status :: any)
		end

		if self.code ~= nil and self.code ~= "" then
			output.code = self.code
		end

		return output
	end

	function _BrowserFlowPollResponseImpl.jsonDecode(input: { [string]: any }): BrowserFlowPollResponse
		local self = _BrowserFlowPollResponseImpl.new()

		if input.status ~= nil then
			self.status = if typeof(input.status) == "number"
				then (messages.BrowserFlowStatus.fromNumber(input.status) or input.status)
				else (messages.BrowserFlowStatus.fromName(input.status) or input.status)
		end

		if input.code ~= nil then
			self.code = input.code
		end

		return self
	end

	_BrowserFlowPollResponseImpl.descriptor = {
		name = "BrowserFlowPollResponse",
		fullName = "api.v1.BrowserFlowPollResponse",
	}

	messages.BrowserFlowPollResponse = _BrowserFlowPollResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.BrowserFlowPollResponse)
end

messages.AccountLinkFlow = {
	fromNumber = function(value: number): AccountLinkFlow?
		if value == 0 then
			return "ACCOUNT_LINK_FLOW_UNSPECIFIED"
		elseif value == 1 then
			return "ACCOUNT_LINK_FLOW_DEVICE_CODE"
		elseif value == 2 then
			return "ACCOUNT_LINK_FLOW_BROWSER"
		else
			return nil
		end
	end,

	toNumber = function(self: AccountLinkFlow): number
		if self == "ACCOUNT_LINK_FLOW_UNSPECIFIED" then
			return 0
		elseif self == "ACCOUNT_LINK_FLOW_DEVICE_CODE" then
			return 1
		elseif self == "ACCOUNT_LINK_FLOW_BROWSER" then
			return 2
		else
			return self
		end
	end,

	fromName = function(name: string): AccountLinkFlow?
		if name == "ACCOUNT_LINK_FLOW_UNSPECIFIED" then
			return "ACCOUNT_LINK_FLOW_UNSPECIFIED"
		elseif name == "ACCOUNT_LINK_FLOW_DEVICE_CODE" then
			return "ACCOUNT_LINK_FLOW_DEVICE_CODE"
		elseif name == "ACCOUNT_LINK_FLOW_BROWSER" then
			return "ACCOUNT_LINK_FLOW_BROWSER"
		else
			return nil
		end
	end,
}

messages.BrowserFlowStatus = {
	fromNumber = function(value: number): BrowserFlowStatus?
		if value == 0 then
			return "BROWSER_FLOW_STATUS_UNSPECIFIED"
		elseif value == 1 then
			return "BROWSER_FLOW_STATUS_PENDING"
		elseif value == 2 then
			return "BROWSER_FLOW_STATUS_COMPLETE"
		elseif value == 3 then
			return "BROWSER_FLOW_STATUS_DENIED"
		elseif value == 4 then
			return "BROWSER_FLOW_STATUS_EXPIRED"
		else
			return nil
		end
	end,

	toNumber = function(self: BrowserFlowStatus): number
		if self == "BROWSER_FLOW_STATUS_UNSPECIFIED" then
			return 0
		elseif self == "BROWSER_FLOW_STATUS_PENDING" then
			return 1
		elseif self == "BROWSER_FLOW_STATUS_COMPLETE" then
			return 2
		elseif self == "BROWSER_FLOW_STATUS_DENIED" then
			return 3
		elseif self == "BROWSER_FLOW_STATUS_EXPIRED" then
			return 4
		else
			return self
		end
	end,

	fromName = function(name: string): BrowserFlowStatus?
		if name == "BROWSER_FLOW_STATUS_UNSPECIFIED" then
			return "BROWSER_FLOW_STATUS_UNSPECIFIED"
		elseif name == "BROWSER_FLOW_STATUS_PENDING" then
			return "BROWSER_FLOW_STATUS_PENDING"
		elseif name == "BROWSER_FLOW_STATUS_COMPLETE" then
			return "BROWSER_FLOW_STATUS_COMPLETE"
		elseif name == "BROWSER_FLOW_STATUS_DENIED" then
			return "BROWSER_FLOW_STATUS_DENIED"
		elseif name == "BROWSER_FLOW_STATUS_EXPIRED" then
			return "BROWSER_FLOW_STATUS_EXPIRED"
		else
			return nil
		end
	end,
}

return {
	TelemetryRequest = messages.TelemetryRequest,
	PluginLoaded = messages.PluginLoaded,
	UiOpened = messages.UiOpened,
	OnboardingCompleted = messages.OnboardingCompleted,
	AccountLinkStarted = messages.AccountLinkStarted,
	AccountLinked = messages.AccountLinked,
	DeviceCodeFlowFailed = messages.DeviceCodeFlowFailed,
	BrowserFlowFailed = messages.BrowserFlowFailed,
	AccountRemoved = messages.AccountRemoved,
	PresenceToggled = messages.PresenceToggled,
	ProfileSelected = messages.ProfileSelected,
	SessionError = messages.SessionError,
	PluginUnloaded = messages.PluginUnloaded,
	BrowserFlowStartRequest = messages.BrowserFlowStartRequest,
	BrowserFlowStartResponse = messages.BrowserFlowStartResponse,
	BrowserFlowPollRequest = messages.BrowserFlowPollRequest,
	BrowserFlowPollResponse = messages.BrowserFlowPollResponse,
	AccountLinkFlow = messages.AccountLinkFlow,
	BrowserFlowStatus = messages.BrowserFlowStatus,
}
